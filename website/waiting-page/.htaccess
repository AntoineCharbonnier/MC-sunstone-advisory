# Auth Basic sur ce dossier uniquement (nécessite .htpasswd à côté de ce fichier)
AuthType Basic
AuthName "Sunstone Advisory — Preview"
AuthUserFile /home/sunstob/www/waiting-page/.htpasswd
Require valid-user

# Protect .htpasswd from direct web access
<Files ".htpasswd">
    Require all denied
</Files>
